#include <stdio.h>

/**
 * _puts - printrs a string to the standard output followed by a newline
 *
 * @str: parameter to be printed
 *
 * Return: nothing
 */

void _puts(char *str);
{
	printf("%s\n",str);
}

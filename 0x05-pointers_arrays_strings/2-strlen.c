#include <stdio.h>
#include <string.h>

/**
 * _strlen - takess a string as argument and return it length
 *
 * @s: first parameter
 *
 * Return: int
 */

int _strlen(char *s)
{
	int len = strlen(*s);

	return (len);
}

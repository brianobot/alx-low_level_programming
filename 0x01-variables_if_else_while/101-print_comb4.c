#include <stdio.h>

/**
 * main - starts program execution
 *
 * Return: integer
 */

int main(void)
{
	int base = 0;
	int i, j, k;

	for (i = base; i < (base + 10); ++i)
	{
		for (j = base + 1; j < (base + 10); ++j)
		{
			for (k = base + 
		}
	}
	return (0);
}

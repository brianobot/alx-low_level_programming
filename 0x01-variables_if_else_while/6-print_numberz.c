#include <stdio.h>

/**
 * main - starts program execution
 *
 * Return: integer
 */

int main(void)
{
	int base = 0;
	int i;

	for (i = 48; i < (base + 10); ++i)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}

#include "main.h"

/**
 * _abs - main entry point
 * description: computes the absolute value of a num
 * @int: integer argument
 *
 * Return: integer
 */

int _asb(int number)
{
	int val;

	if (val >= 0)
		return (number);
	val = (int) number / -1;

	return (val);
}
